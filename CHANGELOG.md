# Changelog

All notable changes to Fermi are recorded here. Releases that pre-date
this file are on the [GitHub Releases page](https://github.com/FelixRuiGao/Fermi/releases).

The release workflow extracts the section whose heading exactly matches
the pushed tag (e.g. `## v0.3.2-alpha.3`) and uses it as the GitHub
Release notes. A missing or empty section fails CI.

## Unreleased

## v0.4.1-paxton.1

- Synced the fork with upstream Fermi v0.4.1, including agent modes, `/goal`, the pure-TypeScript Composer, prompt/tool documentation updates, and model guidance tiers.
- Added `/project` for creating and switching Projects, with a dynamic default path and initial Skill selection.
- Added `/proskills` for exact per-Project Skill profiles while keeping `/skills` global.
- Preserved the voice input and rewrite flow, DashScope Fun-ASR support, macOS local updater/signing, and the fork's existing custom behavior.
- Fixed slow voice transcription dropping later recorder chunks; queued chunks now transcribe in order and pending Enter confirmation resolves after the queue drains.

## v0.4.1

- Fixed: agents no longer try to read the plan/todo file before it exists. The prompts now state explicitly that `plan.md` is agent-created (nothing generates it automatically), auto mode's resume ritual says to read only the files that actually exist, and the auto/scale compact note tells the summarizer not to direct the next instance at files that were never created.
- Changed: reading the plan/todo file now shows as **Check Todos** in the transcript instead of **Update Todos** (which stays for writes/edits).
- Fixed: the input box's bottom status row (permission mode, agent mode, keyboard hints, token usage) no longer word-wraps onto extra lines when the terminal is too narrow to fit everything. It now drops segments by a fixed priority instead — context token usage is never dropped, then permission label, then mode label, then the usage indicator, then the goal timer, then the `(Shift+Tab)`/`(Tab)` keyboard hints (cut first). The priority is a strict chain: if one segment doesn't fit, every lower-priority segment is cut too, even a shorter one that would otherwise fit on its own — so which hint disappears is now deterministic instead of varying with the exact width.
- Fixed: in macOS Terminal.app the letter under the input caret was invisible — Terminal.app paints an opaque block cursor without contrasting the glyph underneath (unlike Ghostty, which redraws it). The composer now draws the grapheme under the caret (including the placeholder's first letter) in a dedicated contrast color, so it stays readable in every terminal.
- Added: **agent modes** — four switchable working stances for the main agent: `default` (balanced everyday collaboration), `vibe` (you steer by outcomes and the agent owns the implementation: it makes technical decisions itself, speaks up when it sees a clearly better option instead of silently complying, verifies by running the actual behavior, and records significant choices in the plan file), `scale` (large multi-part work: writes a design doc and waits for your go-ahead before implementing, brainstorms approaches with parallel explorers, delegates reading but never writing, reviews and compresses context at checkpoint boundaries), and `auto` (long-running self-directed loops: an append-only ledger as ground truth, verify-before-advance units, per-unit context compression, and no stopping to ask whether to continue). Cycle modes with **Tab** and permission modes with **Shift+Tab** (works whether the input box is empty or not, silently — the input area's bottom row now reads `Reversible (Shift+Tab) · <mode> (Tab)` and both labels click-to-cycle); non-default modes also tint the input border. `/mode` remains as a picker. Modes are fully orthogonal to permissions, take effect with the next message, and are safe to switch mid-session: toggling back and forth between sends nets out to nothing, and the model is told exactly once per actual change.
- Added: **/goal** — set a completion condition and the session keeps working across turns until it's met, instead of stopping when a reply ends. The model checks the goal off itself via two new tools (`create_goal`, used only on your explicit request; `update_goal`, which requires verification evidence for `complete` and a same-blocker-for-3-turns discipline for `blocked`) — no judge model and no token budget in this first version; you can interrupt or `/goal clear` at any time, and an active goal survives session resume. `/goal` with no arguments opens a picker (status / clear / replace). Any mode can combine with a goal; auto mode suggests setting one for extended unattended work.
- Added: per-model **guidance tiers** — models default by family (Claude/GPT → standard, everything else → detailed), overridable for custom providers via `models[].guidance` in settings.json. Detailed-tier models get an explicit step-by-step "Context Management — Recipes" section in the system prompt and recipe pointers in the context reminders — weaker models need the moves spelled out where frontier models act on principles.
- Changed: context reminders are now mode-aware — scale/auto sessions nudge earlier (40%/65% instead of 50%/75%, unless you configured levels explicitly, which always wins) and the reminder text points at the mode's natural compression targets (completed checkpoints, recorded ledger units). Auto-compact in scale/auto modes tells the continuation prompt to reference the on-disk design doc / plan / ledger instead of duplicating their contents.
- Changed: the main agent's system prompt was substantially rewritten. New sections cover intent triage (task vs. diagnosis vs. plan — diagnoses no longer auto-apply fixes), working in a shared workspace (never revert changes you didn't make; no destructive git commands or hook-skipping without explicit request), investigate-before-acting, code conventions (match surrounding style, never assume a library is available, no narration comments, no lint-suppression to get to green), scope & simplicity (smallest correct change, no premature abstraction, clean up temp files), verification & honest reporting (every line should run at least once; say what you couldn't verify; no hard-coding values to pass tests), a reversibility/blast-radius taxonomy for risky actions, a general parallel-tool-call policy, and upgraded communication rules (lead with the outcome, report verification evidence, `path:line` references, no emojis). The explorer/worker templates gained matching short sections, and the reviewer template's tool set now matches its "cannot modify files" contract (the edit tools were previously in its schema despite the prompt promising otherwise).
- Fixed: sub-agent system prompts now document exactly the tool set the sub-agent actually has. Previously the generated tool guides were built from the template's declared tools, so a worker/reviewer child's prompt described the background-shell tools (`bash_background`, `bash_output`, `kill_shell`) that are stripped from children at spawn — inviting calls to nonexistent tools — while omitting `await_event`, which children do get. The prompt is now reassembled after the child's final tool set is known.
- Changed: per-tool documentation is now single-sourced in `src/tools/docs/` next to the tool implementations, split into a short contract (rendered into each tool's schema description) and a detailed guide (rendered into a generated "Tools" section of each agent template's system prompt, covering exactly the tools that template has). The four bundled templates previously carried hand-copied tool docs that had already drifted apart from each other and from the implementation; those copies are gone, and custom sub-agent templates now automatically receive current tool guides for whatever tool set they declare — template authors only write the agent's role and policy.
- Added: open-vendor models (Kimi / DeepSeek / GLM / Qwen / MiniMax / MiMo, and anything else that is neither Claude- nor GPT-family) now get a short "Model-Specific Guidance" block appended to the system prompt — read-before-edit, no parallel edits to one file, absolute paths, verify a script exists before running it, batch independent tool calls freely, follow tool schemas exactly. Claude and GPT models are unaffected, and switching models mid-session (`/model`) picks the right block up immediately.
- Changed: the list of available skills moved from the `skill` tool's schema description into an "Available Skills" section of the system prompt. Installing, removing, enabling, or disabling skills no longer mutates the tools array mid-session (the only remaining source of tool-schema change is MCP), which is friendlier to providers that dislike mid-conversation tool-set changes and to prompt caching.
- Changed: the terminal input box is now Fermi's own pure-TypeScript editor instead of the native OpenTUI edit buffer. The cursor is modeled on grapheme boundaries, so the long tail of wide-character bugs (CJK/emoji cursor landing mid-glyph, IME candidate windows drifting, clicks snapping to the wrong half of a wide glyph) is gone by construction rather than patched case by case. Attachments (`@file`, pasted images, collapsed large pastes) are atomic tokens: the cursor can't land inside one, deleting at either edge removes the whole token cleanly, and deleting unrelated text can no longer corrupt them. Mouse behavior is complete (click/drag-select/double-click word/triple-click line, select-to-copy honoring `/autocopy`), wheel scrolling now actually scrolls a tall input instead of snapping back to the cursor, and undo/redo (`Ctrl+-`/`Ctrl+.`, `Cmd+Z`/`Cmd+Shift+Z`) plus select-all (`Cmd+A`) now work in the input box.
- Changed: the small one-line inputs (API-key prompt, the ask panel's custom answer/note field, and the command picker's instructions field) now run on the same self-written editor as the main input box, so they get the same correct wide-character/CJK cursor behavior, and long values scroll horizontally to follow the cursor instead of misbehaving at the right edge. This also fixed a subtle bug where these fields' submit/input handlers could go stale after the UI re-rendered.
- Fixed: in the terminal input box, pressing `Command+Delete` (delete-to-line-start) at the start of the last line — or any delete that removes a line near the bottom — no longer jumps the cursor up onto the previous line when the input has scrolled. The deletion itself was always correct; the bug was a viewport re-clamp that fired right after the edit and dragged the caret to keep it inside the clamped window. The clamp now only pulls the scroll back into bounds and leaves the caret where it is. This only affected tall, scrolled inputs (short inputs were never wrong).

## v0.3.15

- Changed: Fermi now summarizes finished, already-consumed context (large tool outputs, completed exploration, settled findings) on its own initiative as a long session grows, instead of holding off until you explicitly authorize it. This keeps the context window healthy and makes a forced, more-lossy auto-compact far less likely on long tasks. Your own messages are never summarized on Fermi's initiative, an explicit instruction not to summarize (in AGENTS.md or the conversation) is still honored, and summaries remain reversible by rewind.
- Changed: Fermi now leans on `await_event` to wait for a background command or sub-agent to finish, instead of repeatedly polling its output/status — which used to refill the context window with redundant re-reads and bring on the context limit sooner. The minimum `await_event` timeout was also lowered from 15s to 10s so short background waits don't overshoot.
- Fixed: after an automatic context compaction, the self-briefing the model writes to resume work is now more reliable — especially when compaction triggers right after you send a new message (the common boundary case). Both compaction prompts now end with an explicit "what to do first" instruction and both forbid the model from starting the actual work while it writes the briefing; previously only the mid-task variant did either, so a boundary compaction could hand the resumed instance a vague summary with no clear next step (and your just-typed request had already been folded into that summary). The two prompts were also resynced so they differ only where they genuinely must — whether a tool call was interrupted mid-step.
- Changed: Fermi now defaults to brevity in its replies — it skips preamble/postamble (restating the request, "here's what I'll do next," recapping what it just did) while still posting progress notes on long-running work and speaking up with substantive objections. It also no longer stalls for confirmation it doesn't need on investigative tasks like analyzing a problem or diagnosing a bug, where the goal is to reach a conclusion rather than change anything.
- Fixed: stray `console.error`/`console.warn`/`console.log` output (e.g. a tool's internal error stack trace) no longer paints over the terminal UI and corrupts the display. Console output is now redirected to `~/.fermi/stderr.log` alongside stderr; previously only stderr was redirected, but Bun's native console writes bypassed that redirect. Fatal crashes still print to the terminal so you can see why Fermi exited.
- Fixed: `show_context` (and the in-app token/usage estimates) no longer crash with "Disallowed special token found" when the conversation contains literal special-token strings — e.g. `<|im_start|>`/`<|im_end|>` markers leaking in from a Qwen chat template via shell output or read files. Token counts now treat such substrings as ordinary text.
- Fixed: in the terminal input box, clicking into the trailing cell of a double-width grapheme (CJK/emoji) no longer leaves the terminal cursor anchored inside that glyph. Fermi now snaps the terminal cursor used by Ghostty/IME to the grapheme boundary, preventing Chinese IME preedit/candidate UI from overlapping with text that should be covered.

## v0.3.14

- Changed: writing or editing the plan/todo file now appears in the conversation as a single `Update Todos` step instead of being dropped from the transcript entirely. Previously these operations were hidden (only the Todos panel reflected them), so when the file ever landed in the wrong place there was no on-screen sign anything had happened. The step suppresses the path, line-count suffix, and inline diff, since the Todos panel already shows the actual checkpoints. The relabel applies as soon as the target path is known while the call is still streaming, so a plan write is shown as `Update Todos` from the start and never briefly flashes as a regular `Write`/`Edit` with a diff before correcting itself. Detection is by the exact session plan-file path, so a file merely named `plan.md` elsewhere is unaffected. If such a write or edit fails, the error reason stays visible rather than being suppressed along with the diff.
- Changed: the welcome screen wordmark now renders in your theme's accent color (and follows a custom accent if you've set one), instead of a fixed blue-violet. It also shows at every terminal width — previously it was hidden on terminals narrower than 72 columns.
- Changed: Fermi now replies in the language you write in, matching your most recent message, instead of defaulting to English. Code, commands, identifiers, file paths, and established technical terms are kept as-is — only the surrounding prose follows your language.
- Fixed: on Windows, automatic updates (and any download) could hang forever at `Downloading update...` when a proxy was enabled in Windows system settings but no `HTTP_PROXY`/`HTTPS_PROXY` environment variable was set. Bun's networking honors those env vars but does not read the Windows system proxy — the toggle most VPN/proxy clients flip — so requests to blocked hosts like the GitHub release CDN silently stalled with no error. Fermi now reads the Windows system proxy at startup and applies it to every outbound request (provider APIs, web search/fetch, and self-update); an explicit proxy environment variable still takes precedence, and other platforms are unaffected.
- Fixed: a stuck update download no longer leaves the corner toast frozen on `Downloading update...` indefinitely. Downloads now abort if no data arrives for 30 seconds, and a failure is surfaced as an `Update failed — check proxy/network` toast instead of masquerading as an in-progress download.
- Changed: a `skill` tool call now shows in the transcript with its own "Skill" label and the skill's name, instead of the generic tool display.
- Fixed: a skill installed, edited, or removed through the skill manager now takes effect after a reload. The bundled skill-manager instructions wrongly claimed no reload was needed, so a freshly installed skill could stay invisible until some unrelated action triggered a reload; the workflow now reloads explicitly.

## v0.3.13

- Added: `/key` command to manage provider API keys without editing files by hand. Pick a provider endpoint (group providers like Kimi/GLM/Qwen/MiniMax drill down to each region, which has its own key) and replace, remove, or import a detected key. Covers API-key and managed providers plus custom endpoints; OAuth providers stay on `/codex` and `/copilot`. Changes apply to the running session immediately. Removing a key whose value also comes from your shell environment now warns that it will reappear on next launch.
- Changed: `fermi init` now configures the API key before listing models — after you pick a provider endpoint, it asks whether to keep the saved key or replace it, then shows that endpoint's models.
- Changed: `fermi init` now supports Esc (and Ctrl+C) to go back exactly one step at any prompt; backing out of the first step cancels setup. The back hint is shown in each picker's help line.
- Fixed: the agent sometimes wrote its plan/todo file to the project root instead of the session artifacts directory, so the TUI Todos panel stayed empty and checkpoints never reached you. The system prompt named the bare filename `plan.md` several times up front (with no path) but only stated the actual location once, far away in the tool docs — so the model defaulted to the working directory. The planning step no longer names the file at all (it just says to externalize multi-phase work as a live todo list), and the Plan File section now leads with an imperative to create it at the artifacts path, calling out that a `plan.md` written anywhere else is invisible to the TUI.
- Added: text you select with the mouse now auto-copies to the clipboard when you finish the drag (copy-on-select), with a brief "Copied" toast in the top-right corner; the highlight stays so you can re-copy. Toggle it with `/autocopy` (default on); Cmd/Ctrl+C still works too.

## v0.3.12

- Fixed: the conversation flickered with a full-screen redraw at the end of every turn. When the assistant's last message finished, its rendered markdown ran a finalize pass that re-snapped the trailing block's height; because the conversation is pinned to the bottom, that size change re-anchored the scroll and forced a full-viewport repaint every turn. Assistant markdown now stays in streaming render mode after a turn ends (so there's no finalize re-snap), and the streaming height reservation is scoped to only the actively-streaming message — completed messages settle at their true height, with no per-turn repaint.
- Added: skill and MCP availability changes now queue non-waking system notices for the next agent turn. Notices preserve the order of separate user operations, list enabled/disabled skills, and list concrete MCP tool names that became available or unavailable so the agent treats the change as normal runtime configuration instead of assuming its earlier reasoning was wrong.
- Fixed: MCP reload now removes stale `mcp__*` tool schemas and executors after servers are removed or changed, and refreshes same-name schemas from the current MCP manager, so the agent no longer sees tools the current MCP configuration cannot execute correctly.
- Fixed: enabling or disabling an MCP server (and toggling skills) no longer flashes a "Working" indicator in the input box — these are configuration changes, not agent turns. The actual MCP reload is now serialized against the conversation so it can't overlap a running turn.
- Fixed: skill/MCP availability notices now reliably land in the conversation before your next message (in arrival order), and are suppressed entirely before your first message — a config change made before you start chatting never becomes the agent's first context.
- Changed: the MCP "failed to connect" toast no longer disappears after 8 seconds. It stays until the server reconnects (auto-clears) or you dismiss it with Ctrl+L.
- Changed: turning auto-update on with `/autoupdate` now runs an update check immediately, instead of waiting until the next launch.
- Fixed: interrupting a turn now preserves completed thinking once the assistant has started writing normal text; thinking is discarded only if it was still in progress or if the assistant was cut off mid tool-call argument stream. Compact cancellations also clean up consistently now: interrupting automatic or manual `/compact` always records an interrupted turn cleanly and leaves a context note that compaction was canceled.- Fixed: MCP tools broke prompt caching on the first two turns. The `skill` tool was reordered relative to MCP tools between turns, invalidating the provider's cached tools prefix.

## v0.3.11

- Added: `/mcp` and `/hooks` are now interactive picker commands. `/mcp` shows each server with live connection status (✓ green / ✗ red), and offers Reconnect, Disable/Enable, View tools drill-down, and Reload config — the picker hot-updates as servers finish connecting. `/hooks` lists each hook with Disable/Enable toggle and Reload hooks to pick up changes from disk without restarting the session.
- Added: MCP servers connect eagerly at startup (fire-and-forget while you read the welcome screen) instead of blocking on the first message. Failed servers show a dismissible toast in the top-right corner. The first user message appears immediately in the TUI — MCP readiness is awaited after it's written to the log.
- Added: `reload` tool — the agent can call it after writing SKILL.md, AGENTS.md, or MCP config to refresh skills, rebuild the system prompt, and reconfigure MCP servers in one shot, without restarting the session.
- Added: `/usage` command — shows per-section context breakdown (system prompt, conversation, tools, etc.) with token counts and percentages of the context budget.
- Added: `/stat` command — shows all-time token statistics across sessions (total input/output tokens, cache hits, cost estimates).
- Added: Thinking entries in the conversation can be expanded/collapsed inline (click or Enter to toggle). Long thinking blocks start collapsed with a one-line preview.
- Fixed: child-process stderr (from tools, hooks, MCP servers) no longer corrupts the TUI. All stderr output is redirected to a log file (`~/.fermi/logs/stderr-{date}.log`), keeping the terminal clean.
- Fixed: the hand (pointer) mouse cursor over clickable elements — the model name, permission, the Agents/Shells/Todos chips, selectable rows — now works reliably. Two separate issues: (1) after `/resume`, hovering a clickable never changed the cursor. The terminal mouse-pointer escape (OSC 22) is only emitted inside a render tick, and a settled resumed conversation leaves the renderer idle; a bare hover changes no on-screen state, so it scheduled no frame and the escape never went out (it worked on the still-ticking empty/startup screen, hence the "works sometimes" feel). Setting the pointer now requests a render so the change reaches the terminal. (2) After the terminal lost and regained focus, the cursor could intermittently stay stuck on a stale hand until you moused over another clickable and off it. Ghostty records a pointer shape set while unfocused but doesn't repaint the cursor, then de-duplicates the now-identical shape on focus regain — so the corrective reset was a silent no-op. Fermi now never changes the pointer while the terminal is unfocused, so the recorded shape stays consistent with what's on screen; the first hover after refocus re-resolves to a real change and repaints cleanly, with no focus-time fixup needed. (3) Hovering empty screen background — edges, corners, the gaps between panels — showed a hand. The pointer was auto-detected from an element's own mouse-down handler, and the full-screen click-to-dismiss/focus background counts as one; an explicit cursor now wins over that auto-detection, and the background opts out to the default arrow.
- Fixed: opening a `write_file` detail tab while the agent is still streaming a long file (hundreds of lines) made scrolling crawl — and it freed up the instant the write finished. The detail view rendered every line of the file as a live row and re-syntax-highlighted the whole thing on every streamed chunk, which saturated the render thread and starved scrolling. The detail tab is now virtualized (only the rows in view are highlighted and built), and syntax highlighting memoizes per line so an unchanged file body isn't re-tokenized on each chunk. On a 400-line write the per-rebuild cost drops ~16x (`scripts/bench-file-modify-render.ts`); inline diff/write previews benefit from the same per-line highlight cache. Output is unchanged — locked byte-identical to the full render by `opentui-src/components/entry/file-modify-body.virtualize.test.ts`.
- Changed: the custom-provider wizard (`/model → Add custom provider...`) now accepts the full endpoint URL straight from a provider's docs. Pasting `https://openrouter.ai/api/v1/chat/completions` (or any `.../chat/completions` / `.../v1/messages` URL) used to be saved verbatim, after which the SDK appended its own path and every request 404'd; the wizard now strips the recognized suffix down to the base URL the SDK expects. When the suffix identifies the wire protocol it also skips the protocol question — `.../chat/completions` → OpenAI-compatible, `.../v1/messages` → Anthropic-compatible — and tells you what it detected. "Edit endpoint / API key" normalizes the same way.
- Added: four new models, selectable from `/model` — **MiniMax M3** (1M context, multimodal, thinking toggle; on MiniMax Global/China and OpenRouter), **Kimi K2.7 Code** (256K, multimodal, always-on thinking; on Moonshot Global/China/Coding), **GLM-5.2** (1M context; on the GLM Coding Plan endpoints — z.ai's standalone API for it lands next week), and **Qwen3.7 Plus** (1M context, multimodal vision + video input, thinking toggle; on Qwen China/Singapore/US and OpenRouter).
- Changed: upgraded the vendored OpenTUI core/react from 0.3.1 to 0.4.1, including the five prebuilt native targets. 0.4.x moves the Yoga layout engine inside the native library (the old npm `yoga-layout` dependency is gone), so the terminal renderer now ships one less moving part. All Fermi rendering customizations are preserved — cursor handling, sub-cell mouse/scrollbars (SGR-pixels), coalesced markdown spacing, the Agents│Todos split panel, and the `/resume` full repaint — and the behavior is intended to be identical to before. (Internal: see `Docs/opentui-0.4.1-upgrade-plan.md`.)
- Fixed: a few long-standing markdown spacing glitches. Prose immediately followed by a fenced code block, table, or block quote — with no blank line between them in the source — now keeps exactly one separator row, and tables again get their leading separator row. Vertical scrollbar thumbs can also shrink to a half-cell minimum now, matching the horizontal axis.
- Changed: multi-agent sessions release the memory of finished one-shot sub-agents (typically 50-200MB in agent-heavy sessions). A completed one-shot agent's full session — including its log and every tool result in it — used to stay resident until you closed the root session; it is now dropped the moment the agent settles, the Agents panel keeps showing its final state, and opening its transcript tab reads the log back from disk on demand. Persistent agents are untouched and can still be revived.
- Changed: on long sessions, the per-stream-chunk entry patches and per-tool-execution state updates that scanned the whole session log now use verified index lookups (200-300x faster on a 10k-entry log), turning an O(n²)-per-turn cost into O(n). Behavior is locked byte-identical to the old scans by randomized equivalence tests; `scripts/bench-log-hotpaths.ts` documents the measurements.
- Fixed: rewinding past a compaction or summarization boundary no longer leaves holes in the model's context. Compaction archives the pre-compact window's full content to disk; a rewind that removed the compact marker revived those entries with their content still stripped, and the API projection silently skipped them — the model just never saw that part of the conversation again. Rewind now restores the archived content from disk for every summary/compact boundary it truncates.
- Changed: long-session memory use drops. Content covered by a `summarize_context` summary is archived to disk the moment the summary lands instead of staying resident until the next compaction (5-30MB per long session); the TUI transcript poller idles at 1s instead of 250ms when nothing is running, which also stops idle RSS from creeping up; large inline diffs no longer rebuild their hunk structure on every frame.
- Fixed: agents no longer stop "silently" in the GUI / VS Code extension. Four invisible failure modes are closed: (1) turn errors (model 4xx, balance, context overflow) now write an error entry into the session log from the runtime itself — every UI shows them, instead of only the terminal UI which used to write that entry as a client-side courtesy; (2) sub-agent approval requests now reach out-of-process UIs — previously the approval event was derived by polling the root session's log, which a sub-agent's ask never touches, so the UI showed "working" forever while the agent waited for an answer that could never be given; (3) a crashed `fermi --server` process now saves the session, tells the client why it died (`server.crashed`), and the VS Code webview shows an actionable error screen instead of freezing; (4) auto-resume turns (sub-agent completion, shell exit) no longer swallow their errors — they emit the same events and log entries as user-initiated turns.
- Changed: `turn.ended` now reports status `waiting` when a turn parks on a pending approval, instead of claiming `completed`. The VS Code extension shows "Waiting for your approval…" in that state. Unknown statuses degrade to the old behavior on legacy clients.
- Fixed: two turn submissions arriving in the same instant (e.g. two GUI messages in one stdin chunk) could slip past the turn lock and run concurrent model loops, corrupting turn bookkeeping. The lock now claims synchronously and queues claimants strictly in order.
- Added: server-side conversation projection over RPC (`session.getProjectedLog` / `session.getProjectedChildLog`). Remote UIs receive the same canonical conversation structure the terminal UI renders, instead of re-implementing entry pairing/filtering over the raw log — the VS Code extension already consumes it; the raw-log methods remain for older clients. The `ready` event now carries `protocolVersion` and a capability list so clients can feature-detect.

## v0.3.10

- Fixed: self-update on Windows no longer goes through a detached PowerShell helper that waited for fermi to exit and then replaced files — a chain that silently dropped the update whenever any other fermi instance (a GUI tab's server process, the VS Code extension, a second terminal) kept the executable locked, and that relaunched fermi in a separate console window. Updates now install in-process on every platform: files locked by a running image are renamed aside (allowed on Windows, unlike delete/overwrite — the same idiom Claude Code's native installer and rustup use), the new file moves in with rollback on failure, and leftovers are cleaned up on later launches. A failed apply keeps the download and retries on the next launch instead of discarding it. After an update, fermi restarts in the same console.
- Changed: `read_file`'s per-line truncation is now recoverable in-tool. The default cap rose 2000 → 5000 characters (long URLs, wide table rows, and minified-adjacent lines fit untruncated), and a new `max_line_chars` parameter raises it per call — the truncation notice states the longest line's length and the exact parameter value to re-read it in full. The shell-pipeline hint now appears only for lines too long to fit the 80,000-character per-call budget at all. `grep`'s per-line cap stays at 2000 (it's for locating; follow up with `read_file`).
- Fixed: PDF text extraction now preserves the document's line structure (one text line per line, pages separated by blank lines). Previously the extracted view could collapse into a single enormous line, which the per-line cap then truncated to a fragment — forcing the agent into shell workarounds to read the document at all.
- Fixed: reading PDF/DOCX/XLSX files was broken in every release binary — the document converter (markitdown-ts) eagerly loaded jsdom, whose worker lookup the bundler froze into a build-machine absolute path, so any document read on a user machine failed with `Cannot find module ... xhr-sync-worker.js`. Local builds masked the bug because the baked path exists on the build machine. Document projection is now built on direct converters: PDF via unpdf (serverless pdf.js — no worker file, no DOM polyfills), DOCX via mammoth + turndown (same engines as before), and XLSX/PPTX via purpose-built OOXML readers. Nothing in the chain resolves files at runtime anymore.
- Added: PPTX projection now works — slide text, tables, and speaker notes per slide, in presentation order (rearranged decks keep their displayed sequence, not the part files' creation order). It had never worked: the previous converter routed PPTX through an optional dependency that was never installed.
- Fixed: XLSX date cells respect the workbook's 1904 date system (`workbookPr date1904`, common in Mac-origin files) instead of always assuming the 1900 epoch, which would have shifted those dates by four years.
- Changed: dropped markitdown-ts, jsdom, the Vercel AI SDK, and the npm `xlsx` package (frozen at 0.18.5 with two unfixed CVEs — CVE-2023-30533, CVE-2024-22363) from the dependency tree. The binary is ~6 MB smaller, and the pdfjs worker asset is no longer shipped alongside it.
- Changed: Bun runtime upgraded 1.3.9 → 1.3.14 — five releases of upstream fixes baked into the shipped binary, with a Windows-heavy payload (fs.openSync numeric flags, ConPTY groundwork, native win-arm64 toolchain). Version is now pinned in one place (`.bun-version`, read by CI); building from source requires Bun ≥ 1.3.10.
- Fixed: on macOS/Linux, background processes that ignore SIGTERM no longer survive `/clear`, rewind, or session shutdown as orphans — the batch kill path now escalates to a process-group SIGKILL after 1.5s, mirroring what single-shell kill already did (and what Windows' force-kill always did).
- Fixed: `FERMI_OPENTUI_USE_THREAD` was silently ignored on Linux — the renderer forced single-threaded mode over any explicit setting while the startup diag log recorded the value you asked for. Defaults are unchanged on all platforms; the override now actually takes effect.
- Fixed: paths under your home directory now display as `~/...` on Windows/macOS even when the drive letter or profile casing differs, and a sibling directory sharing the home prefix (e.g. `/Users/felixfoo`) is no longer mis-shortened.
- Added: Windows arm64 and Linux arm64 release builds. Snapdragon/ARM Windows machines get a native binary instead of x64 emulation, and Linux arm64 covers Docker on Apple Silicon, ARM servers, and Raspberry Pi. `install.sh` / `install.ps1` now detect the machine architecture (the PowerShell installer reads the real OS architecture, so it picks arm64 even from an emulated x64 shell), the VS Code extension downloads the matching asset, and self-update follows automatically. Release CI gained the two arm64 runners plus a `fermi --version` smoke test on every freshly built binary.

## v0.3.9

- Fixed: sending a message while an approval prompt (or agent question) was pending could permanently break the session — the approved tool silently never ran, and every later turn failed with an API projection error until the session was resumed from disk. The pending ask now keeps the session in a true waiting state: messages queue and are delivered when work resumes, and the approved tool always executes. GUI/VSCode (server mode) were the most exposed since they have no UI-level guard.
- Fixed: pressing stop/interrupt while an approval prompt was pending through the server API (GUI/VSCode) erased the question without an outcome and corrupted the conversation record. All stop entry points now resolve a pending ask as "deny and stop" — same clean wrap-up the TUI's Esc already did.
- Fixed: sending a message to an idle or archived persistent sub-agent crashed its turn immediately (the agent reported `failed` without doing anything). All parent→child deliveries now go through the standard delivery path.
- Fixed: a new message arriving right after an approval was granted (but before work resumed) was silently dropped when sent through `turn`; it is now queued and delivered, and both resume entry points behave identically.
- Changed: user input during context compaction is now rejected with a hint ("Compacting context — try again in a moment") instead of being silently folded into the compaction summary. Automatic messages (sub-agent results, shell exits) arriving mid-compact are held and delivered after the compaction marker.
- Fixed: `/compact` no longer leaves bookkeeping behind that could trigger a ghost auto-turn with no new input.
- Fixed: interrupting the agent right as it summarized context could leak the half-finished summary into a later, unrelated turn.

- Fixed: pressing Enter in a picker's custom-input field (e.g. /review's "Custom review instructions", /summarize_hint's level inputs) did nothing and cleared the field — broken since the feature shipped in v0.3.7. The deferred re-accept ran against the previous render's state and re-entered input mode instead of submitting; Enter now submits directly.
- Fixed: `/review` picker lost note text and custom-input text in every path — the user message bubble showed bare `/review` instead of the user's instructions. Root cause: commands that registered `options` had their picker intercepted before the handler ran; the accept path serialized the result to a string and dropped the note. Unified 7 commands (`/review`, `/summarize_hint`, `/permission`, `/rewind`, `/theme`, `/diff`, `/autoupdate`) to use handler-level `promptCommandPicker` exclusively, which returns structured `{ value, note }` through a resolver. `promptCommandPicker` now accepts an optional `{ title, allowNote }` config.
- Fixed: background shells (dev servers, watchers) are no longer leaked as orphan processes when the TUI exits — quitting, fatal errors, and SIGHUP/SIGTERM now kill all tracked shells before the process dies. Previously the cleanup rode on an un-awaited `session.close()` that never reached its kill step.
- Changed: `/summarize_hint` is now interactive — invoked without arguments it opens a picker (On / Off / Level 1 / Level 2) like other slash commands. Each level is set individually through an inline input whose label and placeholder state the expected range; after setting one level the picker reopens so the other can be adjusted. Inline arguments still work as a shortcut. Custom-input picker options can now declare their own input label/placeholder (previously hardcoded to /review's "Your instructions").
- Added: docs site now has a full Simplified Chinese version under `/zh/`, with browser-language detection on the homepage and a persistent EN/中文 switch in the top navigation.
- Added: Shells panel — a `Shells (N running)` badge above the input box and a `/shells` command open a picker of tracked background shells (`x` stops one, Enter opens a live detail tab with the log tail and a clickable stop button). Clicking a `bash_background` entry in the transcript jumps to the same tab.
- Changed: synchronous `bash` timeouts no longer kill the command — the process is moved to a tracked background shell and keeps running; the tool returns the partial output plus the shell id to poll with `bash_output`. User interrupts still kill. The timeout parameter is now documented as a synchronous wait budget.
- Changed: stopping a shell or killing a sub-agent from the UI no longer wakes an idle agent. The notice is queued and delivered together with whatever wakes the agent next (your next message included) — natural completions and failures still wake it. These pending notices also no longer block `/summarize` and `/compact`.
- Changed: when `/summarize` or `/compact` is blocked by running background shells, the message now lists the shells and points to `/shells`.
- Added: `/summarize_hint` command — manage the two-tier context summarize reminders: `on`/`off`, or set the trigger levels with `/summarize_hint <level1> <level2>` (integers, 0 < level1 < level2 < 85). Persisted in settings.json (`summarize_hint`) and applied live. New defaults: 50% / 75% (was 60% / 80%), and the reminder prompts now ask for the user's permission before the agent summarizes on its own — autonomous summarization requires consent given in conversation or a standing policy in AGENTS.md.
- Changed: summaries are now ordinary context — any summary (including /summarize-created ones) can be re-summarized and merged like other context groups. A summary belongs to the turn of the nearest preceding user message, so the /summarize picker is a pure turn list (no separate "(Summary)" entries), and adjacent summaries whose covered turns are gone can be merged in one operation.
- Changed: when /summarize covers user messages, their original words are preserved verbatim inside `<user-message>` blocks in the summary (attached file contents are still summarized as data); re-summarization carries these blocks through unchanged. The "exceptionally long user message may be tightened" exception is removed.
- Changed: system notices and peer messages are no longer protected like user messages — the agent may summarize them; only the user's own messages anchor turns and stay untouchable.
- Refactored: session runtime split into 7 sub-modules (`src/session/`) — rewind engine, session log store, context manager, compact prompts, sub-agent factory, child session manager, and session persistence. `session.ts` reduced from ~8100 to ~5865 lines. Session restore no longer constructs a shadow Session; the restore pipeline is now pure-parse (`parseRestoredState`) + single-pass apply, sharing log surgery functions with the live interrupt path. No user-facing behavior change.

## v0.3.7

- Added: system prompt now shows the model identity (e.g. "Anthropic/Claude Sonnet 4") and session start time in the opening paragraph — the model knows who it is before reading any instructions. Model name is snapshotted at session creation and stable across `/model` switches and resumes.
- Added: command picker custom-input options — options marked `customInput` open an inline text field on Enter (like the ask panel's "Enter custom answer"). `/review`'s "Custom review instructions" now uses this instead of showing an error.
- Changed: picker note input (Tab to add instructions) is now opt-in per command (`allowPickerNote`); only `/review` enables it. Other pickers (model selection, `/resume`, etc.) no longer show the Tab hint.
- Changed: unified overlay/panel UI colors — replaced yellow with accent color across command picker, ask panel, prompt select, prompt secret, and OAuth overlays. Yellow is now reserved for interrupted-turn system entries only.
- Fixed: GitHub Copilot model selection broke after GitHub's 2026-06-01 usage-based-billing migration. The `/model` picker now follows your plan's live `/models` catalog — models GitHub no longer offers you are hidden instead of failing with `model_not_available_for_integrator` (HTTP 400) — routing is by model family so Claude Opus 4.7/4.8 and GPT-5.5 no longer throw "Unknown Copilot model", and the gateway API version is bumped to `2026-06-01`.
- Removed: Copilot per-model premium-request "multiplier" labels and the Copilot usage card. Copilot moved to usage-based billing (AI Credits, charged by token), so the legacy premium-request quota no longer applies to most accounts.
- Fixed (security): on case-insensitive filesystems (default macOS, Windows Git Bash) uppercase or wrapper-prefixed dangerous commands (`RM -rf ~`, `ENV rm -rf ~`, `NICE rm -rf ~`) bypassed the danger/catastrophic gate and could auto-run — command-name matching is now case-folded at every layer (wrapper stripping, classification, and the cp/mv escalation). Windows `format`/`diskpart` and PowerShell `Format-Volume`/`Clear-Disk`/`Remove-Partition` now classify as catastrophic (still prompted in yolo mode).
- Fixed: Windows self-update never completed — the staged-update PowerShell helper aborted before copying files because its inner function reused the read-only automatic variable `$Pid` as a parameter, and `Start-Process -ArgumentList @()` is rejected by Windows PowerShell 5.1 on the common no-extra-args relaunch. Renamed the parameter and conditionally omit `-ArgumentList` when empty.
- Fixed: on Windows, shell and `bash`-tool spawns could intermittently lose `git`/`node`/`npm` from `PATH` — the env builder injected a second, truncated `PATH` key alongside the inherited `Path` (case-mismatch) and the truncated one could win. The guard is now case-insensitive.
- Fixed: Windows background processes (`npm run dev`, vite, watchers) leaked as orphans on exit / `/clear` / rewind because `taskkill` was sent without `/F` (a no-op for windowless console processes). It now always force-kills the whole tree.
- Fixed: editing CRLF (Windows-authored) files — `read_file` shows LF-only content, so a multi-line snippet pasted back as `old_str` never matched the CRLF file. `write_file`, `edit_file`, and append now match against and preserve the file's existing line endings, with majority-vote EOL detection so a lone stray CRLF doesn't flip an LF file.
- Fixed: Linux clipboard now cascades wl-clipboard → xclip → xsel → OSC 52 instead of locking to one tool by display server (Wayland sessions with only xclip installed previously failed). OSC 52 wraps for GNU screen, sends raw through tmux so tmux's own `set-clipboard` forwards it, and reports honest failure when stderr isn't a TTY. Windows clipboard image reads (Ctrl+V) no longer freeze the UI (async spawn).
- Fixed: Windows `@`-file completion (backslash/slash normalization), PowerShell non-ASCII output mojibake (forces UTF-8 console encoding for both Windows PowerShell 5.1 and pwsh 7+, while leaving `using`/`param`-leading commands untouched), and hooks invoking `.cmd`/`.bat` shims (npm/npx/prettier) now run through the shell while native `.exe` hooks bypass it. Ctrl+Y added as an image-paste shortcut for Windows Terminal (which consumes Ctrl+V).
- Fixed: external-path permission rules are matched case-insensitively on case-insensitive filesystems, so an approved location isn't re-prompted or hard-failed due to drive-letter/path casing.
- Changed: all three prebuilt native packages (`@opentui/core-*`) are now optional dependencies, so install no longer fails on platforms without a published prebuilt; unsupported platforms get a clear "not supported" message while genuine load failures on supported platforms surface their real error. The `read_file` long-line hint now adapts to the active shell (bash vs PowerShell).

## v0.3.6

- Fixed: Windows startup deadlock — the staged-update PowerShell helper had `$ErrorActionPreference` before `param()` (a parse error), causing the handoff to silently fail and `~/.fermi/staged/` to persist, locking every subsequent launch into a silent `process.exit(0)`. Also renamed `$Args` to avoid shadowing PowerShell's automatic variable.
- Added: handoff marker guard (`.update-handoff-pending`) so a failed PS1 handoff self-heals on the next launch instead of looping forever.
- Added: `/review [prompt]` slash command for code review. Picker offers Uncommitted changes, Against base branch, Specific commit, or Custom instructions. Review prompt uses P0–P3 severity levels and 8-point bug qualification criteria.
- Added: command picker now supports inline note input — press Tab on any option to attach instructions (used by `/review` for adding focus to a selected review target).
- Removed: `csv-data`, `explain-code`, `tui-test` bundled skills (prompt-only, no non-obvious knowledge).
- Changed: `/review` and skill slash commands now show a short display line in the conversation (e.g. `/review uncommitted changes`) instead of exposing the full prompt template.
- Fixed: injected-turn commands (`/review`, skills) now correctly surface tool-approval dialogs instead of silently hanging.
- Fixed: `/review <word>` no longer misidentifies single-word instructions as branch names; uses `git rev-parse` to verify refs.

## v0.3.5

- Added: init wizard now prompts for a web search API key (Serper recommended — Google index, 2,500 free queries/month). Pasted keys are saved to `~/.fermi/.env` alongside provider keys. Users who skip get the built-in free fallback chain.
- Added: Parallel Web Search MCP as a new fallback in the client-side search chain. The priority is now: API key backends → Exa free MCP → Parallel free MCP → DuckDuckGo lite. Parallel runs its own web-scale index and requires no API key or registration.
- Changed: upgraded vendored OpenTUI fork from 0.2.1 to 0.3.1. Native binaries (`@opentui/core-*`) and `@opentui/react` synced to 0.3.1. All Fermi-specific patches preserved. Upstream gains include blockquote/list/hr markdown renderables, Code `baseHighlight`, platform FFI abstraction, stdin CPR abort, zellij/remote-shell detection, and Input `minLength`.
- Changed: H1 heading color now matches H2 for a more uniform look.
- Added: VS Code extension (`vscode/`) — a sidebar chat panel that drives the same `fermi --server` backend as the GUI. Streaming markdown, tool-call cards with inline diffs, permission approval, model picker, slash commands, `@file` context from the editor, and an in-webview first-run setup wizard. Sessions are shared with the TUI (same on-disk storage), so conversations started in the terminal appear in the extension's history and vice versa; clicking one opens it in an editor tab. Works over Remote SSH (runs on the remote host) and offers one-click install when the `fermi` binary is missing. Marketplace-publishable (no proposed APIs).
- Added: `fermi sessions [--json] [--work-dir <path>]` lists saved sessions for a project directory (reads on-disk storage without starting a server). Used by the VS Code extension's history list.
- Changed: `fermi --server` now enters an interactive setup mode (instead of erroring) when no providers are configured, so GUI/extension clients can drive the init wizard over RPC rather than requiring `fermi init` in a terminal first.

## v0.3.4

- Added: Windows PowerShell support as fallback shell. Fermi no longer requires Git for Windows — when Git Bash is unavailable, it falls back to pwsh (PowerShell 7+) or powershell (Windows PowerShell 5.1). Shell detection is fully automatic with priority Git Bash → pwsh → powershell. The `bash` tool description and system prompt dynamically adapt to the active shell, guiding the model to write correct PowerShell syntax (cmdlet names, `$env:VAR`, chaining rules, etc.). The permission classifier uses tree-sitter-powershell for AST-accurate command classification with PowerShell-specific command sets, alias resolution (60+ entries), script-block/subexpression detection, `-EncodedCommand` prefix matching, and catastrophic-path checks for `Remove-Item -Recurse -Force` on drive roots and home directories. The TUI shows a non-blocking hint when running in PowerShell fallback mode, suggesting Git for Windows installation for better compatibility.
- Changed: codex (`openai-codex`) requests now mirror the official Codex CLI (`codex_cli_rs`) wire contract instead of an experimental opencode spoof. The request carries an honest `originator: fermi`, a `fermi/<version> (<os> <release>; <arch>)` `User-Agent` (version lives only in the UA — the CLI sends no separate `version` header), the real `ChatGPT-Account-Id` extracted from the OAuth access token's JWT, and `session_id` carrying the prompt-cache key. The non-standard `conversation_id` header (which the CLI never sends) was dropped. Verifying against the open-source Codex CLI showed the backend accepts arbitrary `originator` values (`codex_cli_rs`, `opencode`, `openclaw` all work), so the spoof had no benefit.
- Changed: codex requests now strip fields the Codex backend silently degrades on rather than rejecting — `temperature`, `max_output_tokens`, `top_p`, `metadata`, `prompt_cache_retention` — none of which the official Codex CLI sends. These were already skipped on the default path, but a user-supplied `extra` block could reintroduce them; the strip now runs last so nothing leaks through. Fields the CLI does send (`prompt_cache_key`, `service_tier`, `text`, `reasoning`, `include`, `store`) are preserved.
- Fixed: encrypted reasoning was dropped on the OpenAI Codex backend, so the chain of thought was never carried across tool-call rounds. The streaming parser only reconstructed reasoning from `response.completed.output`, but the codex backend (`chatgpt.com/backend-api/codex`) returns an empty `completed.output` and delivers the encrypted reasoning item only through `response.output_item.added/done` events. Reasoning capture now reads those streamed output items (matching the Vercel AI SDK's approach) for both `openai` and `openai-codex`, so `reasoning.encrypted_content` is preserved and replayed. The reasoning round-trip payload is now reasoning-only — function calls are replayed via the existing `tool_calls` path instead of being bundled into the sealed payload (which previously double-sent them); old logs that stored `[reasoning, function_call]` payloads still replay correctly.
- Fixed: the OpenAI (`provider: "openai"`) Responses path lost its reasoning between tool-call rounds and missed reasoning-related cache hits. Fermi manages the conversation client-side and never sends `previous_response_id`, but the openai provider was left on the default `store: true` *without* requesting `include: ["reasoning.encrypted_content"]` — so server-side reasoning was never read back (no `previous_response_id`) and no encrypted reasoning was echoed either, leaving only the lossy `summary`, which the backend discards. The openai provider now goes fully client-managed stateless like `openai-codex` and `copilot`: it forces `store: false`, requests encrypted reasoning, and replays the sanitized encrypted `reasoning` round-trip items each turn. This preserves the chain of thought across tool rounds and restores reasoning-token cache reuse. `openai` and `openai-codex` now take the exact same reasoning path (OpenRouter stays separate because of its Fernet re-encryption envelope).
- Providers / model picker: added native Qwen / DashScope support on the OpenAI-compatible Responses path with three managed regional endpoints (`qwen` China, `qwen-intl` Singapore, `qwen-us` Virginia), plus OpenRouter presets for `qwen/qwen3.6-plus` and `qwen/qwen3.7-max`. Qwen direct defaults now use DashScope's documented Responses base URLs under `/api/v2/apps/protocols/compatible-mode/v1`; request shaping uses DashScope Responses controls (`enable_thinking` and built-in `{ type: "web_search" }`), replays prior plaintext thinking as Responses `reasoning` items, captures streamed `web_search_call` sources as citations, routes detected `DASHSCOPE_*` / `QWEN_*` keys through the managed import flow before treating the provider as configured, capability tables know the two models (1M context, thinking, Qwen3.6 Plus multimodal), and TUI model labels/vendor buckets render them with beautified `Qwen` names instead of raw ids.
- Fixed: queuing a follow-up message while the assistant was mid-turn could later crash a long session with a provider 400 (`messages[N].content: invalid type: sequence, expected a string` on DeepSeek and other Anthropic-protocol backends). Draining the queued message advanced the turn counter while a provider round was still in flight, so that round's streamed reasoning was stamped with a different turn index than its own tool calls; the projection then grouped the reasoning into a standalone, content-less "thinking-only" assistant message that strict backends reject. Each activation now stamps all of its log entries (reasoning, text, tool calls, etc.) with a single turn index captured at the start, so a mid-flight drain can no longer split a round. Sessions already broken by this are self-healed on load: an orphaned reasoning entry whose round's real entries sit under one other turn is re-stamped to match.
- Added: tool-input repair for open models. Recoverable argument-shape mistakes that DeepSeek / GLM / Qwen / Kimi routinely make are now fixed at the exact failing argument instead of being rejected — a stringified array (`'["a","b"]'`), an object placeholder, or a bare string where a list was expected are coerced to the right shape; a markdown auto-link leaked into a file path (`"[notes.md](http://notes.md)"`) is unwrapped to the plain path. Repairs only run after the as-is value fails validation, so valid inputs (including `write_file` content that merely looks like JSON or markdown) are never touched.

## v0.3.3

- TUI: added `/diff` to choose write/edit diff display. Compact remains the default; Full persists globally and expands file-modify blocks inline instead of showing the earlier-lines fold prompt.
- TUI: markdown H1/H2 heading colors changed from orange/gold to blue tones (closer to the accent palette).
- Fixed: model selection was not remembered across restarts — switching with `/model` always reverted to the model picked during first-time setup. The init wizard wrote that initial pick into `settings.json` as `default_model`, which is a *declarative pin* that overrides `state/model-selection.json` (the system-managed last-used memory) on every startup; `/model` only updates the latter, so the pin always won. The wizard no longer writes `default_model` — the initial selection is still persisted to `model-selection.json`, and `default_model` is now purely opt-in for users who want to hard-pin a model by hand. Existing `settings.json` files that already contain an auto-written `default_model` should remove that line for model memory to take effect.
- Fixed: Kimi (Moonshot) on the Anthropic transport crashed every web-search turn with `JSON Parse error: Unexpected EOF`. On a degenerate (empty) search — which Kimi K2.6 prepends to even trivial turns like a bare greeting — Kimi emits an `input_json_delta` SSE event with **no** `partial_json` field, and the Anthropic SDK's stream accumulator computes `"" + undefined === "undefined"` and feeds that to its partial-JSON parser, which throws and kills the whole turn. Kimi's client now wraps fetch with a small SSE repair (`makeAnthropicSSERepairFetch`, driven by `TransformStream`/`pipeThrough`) that normalizes any `partial_json`-less `input_json_delta` to an empty string before the SDK sees it; real searches (which carry a proper `partial_json`) and every compliant vendor pass through untouched. Real Kimi web search now completes end to end and returns live results.
- Fixed: Kimi's leading "Search results for query: ..." preamble was suppressed from the *saved* transcript but still streamed to the live TUI on every search turn. `_callStream` now buffers a leading text block and drops it if a `server_tool_use` web search immediately follows (Kimi-only, gated on web-search support), so the preamble never reaches the live stream; the real answer — which always follows thinking/search — still streams incrementally.
- Prompt caching: on the Anthropic provider, `cache_control` breakpoints now cover the tool definitions and the last message in addition to the system block (previously only the system block), so tool schemas and the conversation prefix are cached too, not just the system text. Session-specific paths (`{PROJECT_ROOT}` / `{SESSION_ARTIFACTS}` / `{SYSTEM_DATA}`) are substituted inline with their real absolute values, so tool-call examples and instructions always show concrete paths the agent can use directly; the substituted prompt is stable within a session (and across sessions in the same project), so it stays cache-friendly turn-to-turn.
- Prompt: a short `# Session Configuration` section is appended at the very end of the system prompt, carrying the session start timestamp (rendered in the runtime's local timezone). It is labelled as the time of the first message — explicitly the *start* time, not necessarily "now", since a session can be resumed after an arbitrary gap — and points the agent at the `time` tool for the current time. It is provided as ambient context only; the agent is not instructed to comment on it. Spawned sub-agents each append exactly one such section with their own start time — the parent does not pre-render it into the child's template prompt.
- Agent prompts: reworked the main agent prompt. Dropped the separate communication/tone section; kept the four-phase workflow (Explore → Plan → Act → Review, iterative) with guidance to delegate bulk exploration to `explorer` sub-agents and protect their independent context, and added a note to reach for external sources (web search, official docs, discussions) when stuck instead of retrying in a vacuum. A "Your judgment" section covers contributing views, pushing back before committing to the user's call, and building only what was agreed; a "Mindset" note covers staying honest under pressure and never fabricating success. Sub-agent delegation guidance distinguishes orienting context (goal, constraints, facts) from contaminating a fresh sub-agent with your own conclusions and dead ends, and reviewers read `git diff` as the unbiased source of truth instead of a prose summary.- TUI: quitting fermi no longer leaves SGR mouse-event garbage (e.g. `51;790;1276M`) on the shell prompt when mouse motion was occurring during exit. The fix has three layers: (1) destroy now writes `?1000l/?1002l/?1003l/?1006l` to actually stop mouse reporting — upstream OpenTUI 0.2.1 omitted this and got away with it because modern terminals like iTerm2/Alacritty/Kitty/Ghostty implicitly clear mouse modes when the alt-screen client exits, but macOS Terminal.app does not; (2) fermi's own SGR-Pixels feature added `?1016h` for sub-cell scrollbar drag but never paired it with `?1016l` on shutdown, so SGR-Pixel framing stayed armed (zig's setMouseMode deliberately ignores `?1016`); and (3) even after writing every reset CSI, one or two bytes still leaked because mouse-motion reports already sitting in the kernel TTY input buffer were inherited by the shell when raw mode was restored. `main.tsx` now resets mouse tracking, waits ~30 ms so the kernel pumps in-flight bytes up into node where the still-attached stdin parser consumes them, drains node's readable buffer, and only then destroys the renderer. All reset CSIs go directly through the TTY rather than zig's `writeOutBuf` to avoid being dropped when the render thread suspends.
- TUI: prompt history navigation now only triggers at absolute composer boundaries. After recalling a history item, ↑/↓ first move through the recalled text normally; history only advances again from the start/end of the composer, while the existing live-draft restore semantics are preserved.
- TUI: OpenAI Codex OAuth token refresh is now active-model lazy instead of scanning every configured `openai-codex` entry during startup. If refresh fails, the error is written into the conversation as a normal session error entry instead of leaking through stderr into the TUI input area. Switching or logging into Codex also invalidates cached model configs so the active provider picks up the fresh access token.
- Model switching: `/model` now switches the active model in place instead of resetting into a fresh conversation. Thinking history is tracked on three independent axes — transport protocol (`responses` / `anthropic` / `chat`), encryption family (`openai` / `anthropic` / `none`), and sealed wire schema (`anthropic-messages` / `openai-responses` / `openrouter-chat`). Sealed reasoning round-trips only when **both** the encryption family and the schema match: Anthropic direct and Copilot Anthropic share `anthropic-messages` (verified empirically), OpenAI direct / Copilot Responses / openai-codex share `openai-responses`, but OpenRouter wraps reasoning in its own Fernet-encrypted envelope (`openrouter-chat`) that is not interchangeable with direct OpenAI even when both target the OpenAI family. Cross-family or cross-schema sealed reasoning is omitted; non-encrypted targets receive a plain-text replay of the prior thinking summary. Legacy OpenRouter sessions saved before this change now have their `reasoning_details` recognized on first replay via shape inference (encryption hint inferred from `signature` / `reasoning.encrypted` entries).
- Providers: restored Kimi/Moonshot native Anthropic web search (`web_search_20250305`) on the new Anthropic transport path, unified provider default base URL fallbacks in a shared table (including `kimi-code`), tightened Anthropic Messages request shaping so Chat/Responses-only fields like `extra_body`, `reasoning_effort`, `web_search_options`, `top_p`, and `top_k` no longer leak into Anthropic requests, and filtered the leading "Search results for query: ..." preamble text block that Kimi emits before `server_tool_use` (the preamble is now dropped from the agent's text field so the saved transcript only contains the real answer).
- Providers: migrated Kimi/Moonshot, MiniMax, DeepSeek, and Xiaomi MiMo from OpenAI-chat-compatible adapters to Anthropic Messages-compatible adapters, with new `/anthropic` default base URLs, shared Anthropic protocol handling, vendor-specific thinking controls, and deprecated rollback stubs for the old chat adapters.
- Tools: improved client-side `web_search` / `web_fetch`. Exa MCP search fallback now parses structured text into individual titled URL results with multiline `Highlights:` blocks instead of returning one aggregated empty-URL result or collapsing evidence to the first line; long highlights are soft-truncated at nearby word boundaries. `web_fetch` still uses Jina Reader first, but local fallback now rejects local/private targets, limits redirects, converts HTML through Readability + Turndown, and normalizes successful output to `# Content from <url>` plus readable page content.
- TUI: assistant markdown now renders with top-level block boundaries while reusing same-kind renderables during streaming. This keeps the top-level one-gap spacing model for headings, paragraphs, tables, code blocks, and blockquotes, avoids stale height floors when an incomplete table paragraph becomes a real table, and prevents the old top-level flicker caused by destroying/recreating ordinary text blocks on every streamed update.
- Auto-update: unified all platforms on a staged-update flow. `fermi update` now downloads and stages the latest release for the next restart instead of replacing the live binary inline. Release asset naming and executable names are now sourced from the platform binary-asset metadata rather than updater-local string literals. On Windows, staged updates are handed off to a bundled PowerShell helper that waits for `fermi.exe` to exit before replacing files and relaunching, avoiding self-overwrite of the running executable. Public docs and install/update docs now describe macOS (Apple Silicon), Linux (x86_64), and Windows (x64) consistently.
- `/resume` / `/session`: restoring a session now forces a full screen repaint. A terminal left visually desynced (e.g. a garbled markdown table or a heading "merged" into the table above, after scrolling during streaming) is now cleaned up on resume — previously only a terminal resize recovered it.
- Built-in skills: added 5 bundled document & data skills — `pdf`, `docx`, `xlsx`, `pptx`, and `csv-data`. Invoke any with `/<name>` or let the model pick by description; each installs its Python libraries (pypdf / python-docx / openpyxl / python-pptx / pandas) on demand and degrades gracefully with an install hint if unavailable.

## v0.3.2

- Init wizard: Ctrl+C on the first step now exits cleanly instead of printing a stack trace.
- Init wizard: selecting a model no longer loops back to the picker with a "Done" confirmation — selection proceeds directly to the next step (thinking level). Ctrl+C still goes back.
- Init wizard: sub-agent tier model picker now shows all providers (matching the main model picker), not just already-configured ones. Selecting an unconfigured provider triggers inline credential setup.
- Auto-update overhaul: structured `UpdateState` replaces the old string-based notice system. TUI now shows a hint in the status bar when an update is downloaded (`✓ v0.3.2 ready (restart to apply)`) or available (`v0.3.2 available — run fermi update`). Version comparison correctly handles prerelease suffixes. Tarball downloads are verified against a `.sha256` checksum when available. Binary replacement uses atomic copy→rename. Dev mode is detected and skips auto-apply to prevent writing to wrong directories. Disk version is checked before applying staged updates to prevent duplicate writes from concurrent instances.
- Auto-update: `auto_update` setting now accepts `true` (default), `"notify"` (check but never download), or `false` (disable). With `true`, patch and minor updates auto-download and stage; major updates only notify. This is a behavior change from the previous `true` which auto-downloaded all versions.
- `fermi update --check`: check for updates without downloading or installing. Works in dev mode too (only the install path requires a production install).
- TUI: redesigned welcome screen — the old ASCII block logo (top-left, multi-hue vertical gradient) is replaced by a centered "Fermi Here." text identity with a smooth color-breathing animation (pale blue ↔ blue-purple, compound sine ~4s period). Below it: version, cwd, and key hints (`/` commands · `@` files · `/help` shortcuts) — only information not already shown by the input status bar. The welcome stage is absolutely positioned against terminal height, so it stays perfectly still when slash/command panels open (the old logo jumped because it was centered inside the shrinking scrollbox viewport). ANSI Shadow figlet art kept in theme tokens for potential future use; sidebar wordmark + system entries continue to use their own gradient (decoupled).
- TUI: the scrollbar thumb is now the correct length immediately after `/resume` (or any restore into a previously empty transcript), instead of collapsing to the ~1-cell minimum until the next window resize or slash-command panel toggle. Root cause: on the empty TUI's first layout, `viewport.onSizeChange` fired before `content.onSizeChange`, so `Slider.viewPortSize` was set while `slider.max` was still 0 and got clamped to `0.01` by the `min(size, max - min)` guard; the subsequent content growth updated `max` but the unchanged `ScrollBar.viewportSize` short-circuited its setter, so the stale `0.01` was never re-synced. `slider.viewPortSize` is now assigned inside `updateSliderFromScrollState()` after `slider.max`, so the clamp always sees a current range.
- TUI: markdown after a table (or other separately-rendered block) that begins with a `---` thematic break no longer renders as raw, yellow-tinted text. The remaining markdown was coalesced into a synthetic sub-block starting with `---`, which tree-sitter parsed as YAML front matter — so every heading/list/bold between that `---` and the next was shown as un-concealed directive-coloured text instead of formatted markdown. The synthetic block is now re-highlighted with a leading newline (offsets shifted back) so a leading `---` is parsed as a thematic break, not front matter. Covers the monkey-patched render path the app actually uses, with and without blank-line separators.
- TUI: streaming markdown no longer makes the whole transcript jump up a line and back when an inline span closes (e.g. `**bold**`). The reflow from raw markers to formatted text is horizontal as intended; previously a one-frame height dip in the streaming block — before async highlight/conceal settled — was amplified by sticky-bottom auto-follow into a vertical bounce of the entire transcript. The streaming block now holds a per-wrap-width height floor while streaming (monotonic, reset when the message finalizes), so a transient measurement can no longer shrink it mid-stream.
- TUI: user messages get a faint accent background wash plus a solid 1-column accent bar flush against it, replacing the bar-only marker that was hard to spot while scrolling. The wash is a low-alpha accent (`userWash` palette token, dark and light) — distinct enough to anchor the eye when scanning back through a long transcript, without the weight of the old solid surface. Queued (pending) messages use the same shape, kept distinct by dim text.
- TUI: scrollbar thumb now renders at 1/8-cell precision (eighth-block characters) instead of 1/2-cell, so the thumb glides smoothly along the rail line-by-line instead of jumping in half-cell increments on long conversations. Added a dedicated `scrollbarThumb` palette token alongside the existing `scrollbarTrack` so the rail (now a solid mid-grey instead of empty space) and the thumb (matches body-text colour) are visually distinct. Default minimum thumb size drops from 4 cells to 1 cell, matching the look of other agent terminals like amp; on very long content the thumb is small but stays buttery-smooth as it moves. Dragging the thumb with the mouse is now sub-cell smooth too on terminals that support SGR-Pixels mouse reporting (kitty, Ghostty, WezTerm, iTerm2, recent xterm, VS Code): ScrollBar no longer rounds the scroll position to integer cells, and when the terminal reports the mouse in pixels the position is decoded at fractional-cell resolution. Terminals without pixel mouse reporting (e.g. macOS Terminal.app) fall back to the previous cell-quantized drag with no change in behaviour.
- TUI: switching to a detail tab (clicking a thinking/tool entry) and back no longer resets the transcript to the bottom or clears the composer. The main view now stays mounted with Yoga `Display.None` instead of being unmounted, so scroll position and textarea contents survive tab switches. As part of the same change the composer is now anchored to the bottom of the screen at all times — previously it lived inside the scrollbox and would scroll off-screen when reading history; now scrolling history keeps the input visible, and typing no longer auto-scrolls the transcript to the bottom.
- TUI perf: cut shiki preload from 41 languages to 9 (typescript/tsx/js/jsx, python, bash, json, markdown, diff); everything else lazy-loads on first encounter, briefly flashing a highlight.js fallback while loading. Combined with a synchronous `Bun.gc()` at turn boundaries (throttled to once per 10 s on completed turns), startup peak memory drops by ~130 MB and one observed long session reclaimed ~160 MB the instant a turn ended.
- TUI: body text now follows the terminal's configured foreground (OSC 10) in auto theme mode — so dark/light terminal themes give matching text colour without per-theme tuning. Pinned `FERMI_THEME=light|dark`, or OSC-unresponsive terminals, keep the token-table palette as a contrast-safety fallback.
- TUI: tool results (`list`, `glob`, `grep`, `bash`, `bash_output`, `web_search`, `web_fetch`, `send`, `apply_edits`) now collapse to a single truncated line with a clickable "N more lines, CLICK to open" fold indicator. Errors still expand to 8 lines. Reduces visual noise during long sessions.
- TUI: input prompt `❯` and textarea cursor changed from accent blue to neutral white — works better across terminal palettes and stops fighting with the brand accent.
- Permissions: compound commands with a trailing redirect (e.g. `cd dir && npm install 2>&1`, `pnpm install && pnpm build > build.log`) now offer "Always allow" for the inner command just like the same command without the redirect. Previously the redirect forced a one-time approval.
- GUI: replaced the three separate Models/Skills/Integrations modals with a single unified Settings dialog (left-nav rail + scrollable right content). Six sections: General, Models, Providers, Skills, MCP Servers, Hooks. Opens via the sidebar footer menu or the new `⌘,` shortcut.
- GUI: real CRUD in Settings now writes through to `~/.fermi/settings.json` — Add/Edit/Delete MCP servers via inline form (name / command / args / env / URL); change Default model / Thinking level / Permission mode from dropdowns. Skills toggles use a right-aligned iOS-style switch.
- GUI transcript: `write_file` / `edit_file` tool calls now render as a card with file-type icon, +N/−M diff stats, and an inline unified diff with line numbers and red/green rows (replaces the prior `path +N` chip).
- GUI transcript: code blocks gained a hover "Copy" button. Reasoning blocks ("Thinking") gained a soft left-accent stripe.
- GUI composer: model picker is grouped by provider with brand labels and capability icons (thinking, multimodal); active model badged. Permission picker shows a colored status dot (safe / default / risky). Token counter tints to warning then error as context fills. `@path` reference hint surfaced in the placeholder. `Esc` interrupts a running turn.
- GUI sidebar: top-level "New session ⌘N" button + `⌘N` keybinding; hover pin overlaps fixed; "Show all N" affordance got a chevron.
- GUI session backend: new RPCs `session.getSummarizeTargets` / `session.getContextIdsForTurnRange` so the GUI's Summarize dialog can drive the same picker flow the TUI's `/summarize` command uses.
- GUI session backend: new IPC `settings.upsertMcpServer` / `settings.deleteMcpServer` / `settings.updateDefaults` to support the inline Settings forms above.
- GUI transcript: expanded tool-call output (bash / read_file / etc.) gained a hover "Copy output" button, matching the markdown code-block pattern.
- GUI right-pane empty states: Plan, Agents, and Shells panels each gained a one-line explainer ("checkpoints come from plan.md", "sub-agents appear here when spawned", "bash_background processes appear here") so empty panels are self-documenting.
- GUI right-pane Files: filter input got an X clear button — matches the Settings search inputs.

<!--
Drop entries here as commits land. At release time:
  1. Rename this heading to the tag, e.g. `## v0.3.2` or `## v0.3.2-alpha.3`.
  2. Add a fresh empty `## Unreleased` above.
  3. Commit, then `git tag` and push.
-->
